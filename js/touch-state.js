// touch-state.js — Global touch control state
// This module provides a shared state object for touch button presses
// that the InputManager can always check regardless of which scene is active.

export const touchState = {};
